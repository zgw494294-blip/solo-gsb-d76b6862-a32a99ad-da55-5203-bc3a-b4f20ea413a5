# 复制为 .env 后按需修改（compose 会自动读取 .env）
# 访问端口（宿主机）
PORT=8000
# 带盐哈希的全局盐：生产环境务必替换为随机串，如 openssl rand -hex 32
MASKING_HASH_SALT=please-change-this-dev-salt
# 请求体大小上限（字节），默认 10 MiB
MAX_BODY_BYTES=10485760
